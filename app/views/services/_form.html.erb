<section class= "<%= component %>" >
	<div class="title">
		<h2 class="text">ENCONTROU O QUE PROCURAVA?</h2>
		<div class="line"></div>
	</div>

	<%= form_for :record, :html => {:class => 'form'} do |f| %>

		<div class="box-left">
			<div class="input-group">
				<%= f.label :nome, "NOME"  %>
				<%= f.text_field :name, class: "text-field" %>
			</div>
			<div class="input-group">
				<%= f.label :email, "EMAIL" %>
				<%= f.text_field :email, class: "text-field" %>
			</div>
			<div class="input-group">
				<%= f.label :phone, "TELEFONE" %>
				<%= f.text_field :phone, class: "text-field" %>
			</div>
		</div>
		<div class="box-right">
			<%= f.label :description, "MENSAGEM" %>
			<%= f.text_area :description, class: "textarea" %>
		</div>
		<div class="submit-box">
			<%= button_tag( :class => "submit-button") do %>
				<span>ENVIAR</span>
  			<i class="fa fa-2x  fa-check" aria-hidden="true"></i>
  		<% end %>
		</div>
	<% end %>
</section>
