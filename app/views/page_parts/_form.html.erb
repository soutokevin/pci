<%= form_for(page_part) do |f| %>
  <% if page_part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_part.errors.count, "error") %> prohibited this page_part from being saved:</h2>

      <ul>
      <% page_part.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :allow_creatable %>
    <%= f.check_box :allow_creatable %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
